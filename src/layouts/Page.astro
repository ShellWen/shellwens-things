---
import type { CollectionEntry } from 'astro:content'
import Content from './Content.astro'

type Props = CollectionEntry<'page'>['data'] & {
  slug: string
}

const { slug, title, description, pubDate, updatedDate, heroImage, heroImageAlt } = Astro.props
---

<Content {slug} {title} {description} {pubDate} {updatedDate} {heroImage} {heroImageAlt}>
  <slot />
</Content>
